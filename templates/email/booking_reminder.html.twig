{% extends 'email/email_layout.html.twig' %}

{% block content %}
    <h2>Coming soon!</h2>
    <h1>{{ trip.name }}</h1>
    <p>Departure: {{ booking.date|date('Y-m-d') }}</p>
    <p><a href="{{ url('booking_show', {uid: booking.uid}) }}">Manage Booking</a></p>
    <p><a href="{{ url('bookings', {uid: customer.uid}) }}">My Account</a></p>
    <p>We can't wait to see you there,</p>
    <p>Your friends at Universal Travel</p>
{% endblock %}
